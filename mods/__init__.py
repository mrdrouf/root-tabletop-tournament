"""Root Tabletop Tournament modification package.

Each mNNN_*.py module defines:
    NAME  = "short description"
    apply(text: str) -> str

registry.py lists the enabled modules, in application order.
"""
