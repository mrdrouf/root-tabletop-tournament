# python
__pycache__/
*.pyc

# local machine setup helpers (paths + settings edits specific to this machine)
/setup/

# generator build output (reproduced from gen/src by gen/assemble.py)
gen/build/

# NOTE: dist/ (the compiled self-contained save) is committed on purpose -- the repo is
# self-contained, so GitHub holds the final one-file product AND can rebuild it from gen/src.

# macOS
.DS_Store
